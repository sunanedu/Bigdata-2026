-- Lab 2.1 — แบบฝึกเพิ่มเติม (ทำหลัง Workshop)
-- ฐานข้อมูล: output/ddl_students.db

-- โจทย์ 1: เพิ่มนักเรียน STU006 ชั้น 4 ห้อง ก GPA 3.40
-- (เขียนคำสั่ง INSERT ด้านล่าง)


-- โจทย์ 2: แก้ไข section ของ STU004 เป็น 'ค'
-- (เขียนคำสั่ง UPDATE ด้านล่าง)


-- โจทย์ 3: แสดงเฉพาะ student_id, full_name, gpa เรียง GPA จากมากไปน้อย
-- (เขียนคำสั่ง SELECT ด้านล่าง)


-- โจทย์ 4: ลอง ALTER TABLE เพิ่มคอลัมน์ nickname TEXT
-- แล้ว UPDATE nickname = 'ชาย' ให้ STU001


-- โจทย์ 5: นับจำนวนนักเรียนแยกตาม section
-- (ใช้ GROUP BY)
